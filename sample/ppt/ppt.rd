= Rabbit

: subtitle
  プログラマの

: subtitle
  プレゼンテーションツール

: author
   須藤功平

: allotted-time
   20m

: theme
   ppt

= 自己紹介

  * ((*O*))pen
  * ((*S*))ource
  * ((*S*))oftware
  * ((*プログラマ*))

= Rabbit

  # image
  # src = ../lavie.png
  # relative_height = 100

= PPT

  * ((*P*))rogrammer's
  * ((*P*))resentation
  * ((*T*))ool

= プログラマ

  * 主にUNIX環境に棲息
  * ハッカーと呼ばれることも
  * Emacs/Vi使い

= カスタマイズ好き

  # blockquote
  snip

= いいものはいい

   # blockquote
   すばらしい！

  # blockquote
  カッコいい

= ユーモア

   # blockquote
   # title = 日常の一幕
   うわ，こいつcore吐いた

  # blockquote
  # title = Lispプログラマの日常の一幕
  hungryp

= 要求

  * カスタマイズ性
  * グッとくるもの
  * ユーモア

= 答え

  * ((*カスタマイズ性*))
  * グッとくるもの
  * ユーモア

= Ruby on Rabbit

  # image
  # src = rabbit-with-ruby.png
  # relative_height = 100

= テーマ

  * パラメータ設定
  * 描画

= パラメータ設定

より宣言的に

  # enscript ruby
  prop_set("foreground",
           "red")
  font({:color => "red"})
  font :color => "red"

= 描画

思いっきりプログラム

  # enscript ruby

  n = "XXX.png"
  l = ImageLoader.new(n)
  p = l.pixbuf
  draw_pixbuf(p, x, y)

= 答え

  * (('del:カスタマイズ性'))
  * ((*グッとくるもの*))
  * ユーモア

= グッとくるもの

  * 落描き
  * マウスジェスチャ
  * 検索

= 答え

  * (('del:カスタマイズ性'))
  * (('del:グッとくるもの'))
  * ((*ユーモア*))

= Rabbit-Hole

  # blockquote
  # title = Alice's Adventures in Wonderland
  You take the ((*red pill*)) and you stay in Wonderland and 
  I show you how deep the ((*rabbit-hole*)) goes.

= うさぎとかめ(序盤)

  # image
  # src = hare-and-tortoise-start.png
  # relative_height = 100

= うさぎとかめ(中盤)

  # image
  # src = hare-and-tortoise-middle.png
  # relative_height = 100

= うさぎとかめ(終盤)

  # image
  # src = hare-and-tortoise-end.png
  # relative_height = 100

= 要求

  * カスタマイズ性
  * グッとくるもの
  * ユーモア

= カスタマイズ性

  * ((*本物の*))プログラミング言語

= グッとくるもの

  * 落描き: パレット付
  * マウスジェスチャ: 画像付
  * 検索: Migemo付

= ユーモア

  * Rabbit-Hole
  * うさぎとかめ

= 結論

  * プログラマの

    プレゼンテーションツール

= 現状

  * 受け入れられつつある
    * 一部のRubyistが賛同
    * 海外にもユーザが
  * 共同開発者もいる
  * 魔法の粉は効果ないかも

= 今後の課題

  * メモリ使用量の削減
  * マルチメディアのサポート
  * 印刷品質の向上
  * rabbit-mode.elの提供
  * ドキュメントの整備

= Any Questions?

  * Do you ((*Rabbit*))?

  * Yes! I ((*love it*))!

  # blockquote
  * ((*Rabbit*))してる？

  * もちろんさ！

    俺は((*Rabbitが大好き*))さ！
